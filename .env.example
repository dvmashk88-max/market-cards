# Local runtime
PORT=
NODE_ENV=
LOG_LEVEL=
BASE_PATH=
PUBLIC_APP_URL=

# FazerCards catalog (server-only)
FAZERCARDS_API_BASE=
FAZERCARDS_API_KEY=
FAZERCARDS_REQUEST_TIMEOUT_MS=
FAZERCARDS_CATALOG_CACHE_TTL_MS=

# Server-side catalog pricing
CATALOG_MARKUP_PERCENT=
USD_TO_RUB_RATE=

# Alfa-Bank payment gateway (server-only)
ALFA_API_BASE=
ALFA_USERNAME=
ALFA_PASSWORD=
NODE_EXTRA_CA_CERTS=

# Transactional email (server-only)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_SECURE=

# Persistence, protected order links and encrypted delivery codes (server-only)
DATABASE_URL=
ORDER_ACCESS_TOKEN_SECRET=
ORDER_DATA_ENCRYPTION_KEY=

# Supplier purchase release gate (server-only; keep false until an approved live test)
ENABLE_FAZER_GIFTCARD_ORDERS=
